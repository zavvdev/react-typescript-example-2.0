import { AppTheme } from "view/styles/theme/types/theme";
import { appPalette } from "view/styles/theme/palette";

export const appDarkTheme: AppTheme = {
  palette: appPalette,
  backgroundColor: appPalette.black.main,
};

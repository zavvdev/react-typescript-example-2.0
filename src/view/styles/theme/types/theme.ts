import { AppPalette } from "view/styles/theme/types/palette";
import { ColorType } from "view/styles/theme/types/common";

export interface AppTheme {
  palette: AppPalette;
  backgroundColor: ColorType;
}
